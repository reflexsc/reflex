{
    "config": {
        "app-base": "/app",
        "deploy-base": "/data"
    },
    "setenv": {
    },
    "actions": {
        "launch-monitor": {
            "exec": ["./bin/agent", "start", "monitor", "--cfgin"],
            "type": "system",
            "config": "stdin"
        },
        "update": {
            "type": "script",
			"target": "getreflex.sh"
        },
        "test": {
            "target": "test",
            "type": "script"
        }
    }
}
